# konfiguracja na serwer localhost

server {
    listen 80;
    server_name localhost;


    location / {
        proxy_pass http://website:80;
    }

    location /api {
        proxy_pass http://server:3000;
    }
}

